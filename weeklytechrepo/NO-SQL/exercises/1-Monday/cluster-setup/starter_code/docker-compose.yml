version: '3'
services:
  # The Primary Node
  node-1:
    image: cassandra:latest
    container_name: node-1
    ports:
      - "9042:9042"
    environment:
      - CASSANDRA_CLUSTER_NAME=TrainingCluster
      - MAX_HEAP_SIZE=512M
      - HEAP_NEWSIZE=100M
    
  # The Replica Node
  # TODO: Define node-2 below based on the README instructions.
